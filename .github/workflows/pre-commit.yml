name: Pre-commit

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6.0.2

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install Go tools (goimports, gosec)
        run: make install-pre-commit-tools

      - name: Install ruleguard (needed by gocritic/ruleguard rules)
        run: go install github.com/quasilyte/go-ruleguard/cmd/ruleguard@v0.4.5

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11.4

      - name: Run pre-commit hooks
        uses: pre-commit/action@v3.0.1
